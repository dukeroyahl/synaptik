# Test configuration for Synaptik server
quarkus.mongodb.database=synaptik-test

# Use MongoDB Dev Services for testing (testcontainers)
quarkus.mongodb.devservices.enabled=true

# Log configuration for tests
quarkus.log.level=INFO
quarkus.log.category."org.dukeroyahl.synaptik".level=DEBUG

# Skip MCP server configuration for tests